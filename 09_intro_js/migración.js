function inscribirEnJS(alumno) {
    if (alumno) {
        alumno.curso = 'JS'  
    }
  
}
inscribirEnJS()


function invertir(x) {    // daria 1000 porque es el valor del saldo
    x = x *2
    return x
}
let z = invertir(500)  //500 es la zona de memoria

